# Web framework + extensions
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1

# Database driver + crypto for MySQL 8's default caching_sha2_password auth
PyMySQL==1.1.1
cryptography==43.0.1

# Passwords (bcrypt) + request validation/serialization
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pin: passlib 1.7.4 breaks with bcrypt >=4.1 (removed __about__, 72-byte self-test)
marshmallow==3.21.3

# Object storage (MinIO S3 client)
minio==7.2.7

# Content-type sniffing (pure-Python; no libmagic) + image handling/thumbnails
filetype==1.2.0
Pillow==10.4.0

# WSGI server (prod) + tests
gunicorn==22.0.0
pytest==8.2.2
